All results are stored in results directory, including code outputs

To run with different configurations of data normalization or scoring, change the parameters passed into constructor of QueryEngine
